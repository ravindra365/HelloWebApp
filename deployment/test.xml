<project name="SendMail" default="send-email" basedir="." xmlns:mail="antlib:org.apache.tools.ant.taskdefs.email">

    <!-- Define classpath -->
    <path id="mail.classpath">
        <fileset dir="/var/lib/jenkins/workspace/HelloWorld/deployment/lib">
            <include name="**/*.jar"/>
        </fileset>
    </path>

    <!-- Register mail task -->
    <taskdef name="mail" classname="org.apache.tools.ant.taskdefs.email.EmailTask" classpathref="mail.classpath"/>

    <!-- Target to send mail -->
    <target name="send-email">
        <mail mailhost="smtp.gmail.com"
              mailport="465"
              ssl="true"
              user="yonna.011990@gmail.com"
              password="vzgptqnyzvmfaszx"
              from="yonna.011990@gmail.com"
              tolist="yonna.011990@gmail.com"
              subject="Build Log Attachment"
              message="Here is the attached build log.">
          <!--  <attachments>
                <fileset dir="${basedir}">
                    <include name="build.log"/>
                </fileset>
            </attachments> -->
        </mail>
    </target>
</project>
